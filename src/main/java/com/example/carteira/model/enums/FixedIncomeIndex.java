package com.example.carteira.model.enums;

public enum FixedIncomeIndex {
    PRE_FIXED, // Pre-defined rate
    CDI,       // Brazilian interbank deposit rate
    IPCA       // Brazilian inflation index
}